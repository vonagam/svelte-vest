# Example for @vonagam/svelte-vest-flowbite

Shows a sign-up form controlled by `@vonagam/svelte-vest-flowbite`.

## Usage

Run this and the example should be accessible on `http://localhost:5173/`:

```sh
pnpm install
pnpm start
```
