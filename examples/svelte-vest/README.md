# Example for @vonagam/svelte-vest

Shows an unstyled sign-up form controlled by `@vonagam/svelte-vest`.

## Usage

Run this and the example should be accessible on `http://localhost:5173/`:

```sh
pnpm install
pnpm start
```
